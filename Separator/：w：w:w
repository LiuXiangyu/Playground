function separator(number) {
    number += "";
    var parts = number.split('.');
    parts[0] = parts[0].reverse();
    var strs = [];
    for (var i = 0; i < parts.length; i += 3) {
        strs.push(parts.substr(i, 3));
